import 'package:flutter/material.dart';

/// FIXA brand color palette.
class AppColors {
  AppColors._();

  // Brand
  static const Color primary = Color(0xFFE8A020); // amber/orange
  static const Color primaryLight = Color(0xFFFFF3E0);
  static const Color dark = Color(0xFF1A1A2E); // deep navy

  // Surfaces
  static const Color background = Color(0xFFF2F2F7);
  static const Color surface = Color(0xFFFFFFFF);

  // States
  static const Color success = Color(0xFF2E7D32);
  static const Color error = Color(0xFFC62828);

  // Text
  static const Color textPrimary = Color(0xFF1C1C1E);
  static const Color textSecondary = Color(0xFF8E8E93);

  // Misc
  static const Color divider = Color(0xFFE5E5EA);
  static const Color shimmerBase = Color(0xFFEBEBF0);
  static const Color shimmerHighlight = Color(0xFFF5F5F7);
}
